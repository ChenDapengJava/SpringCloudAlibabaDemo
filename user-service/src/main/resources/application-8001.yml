server:
  port: 8001

spring:
  application:
    name: user-service

  cloud:
    nacos:
      discovery:
        # Nacos 集群
        server-addr: 192.168.242.112:81

management:
  endpoints:
    web:
      exposure:
        include: '*'
